#!/bin/bash
#PBS -l nodes=2:ppn=2
#PBS -l walltime=00:02:00
#PBS -N cache_info
#PBS -t 1-100

cd $PBS_O_WORKDIR

# Determine cache size and write to file
for host in $(uniq $PBS_NODEFILE); do
    ssh $host "lscpu | grep 'L3 cache' | awk '{print $3}'" >>cache_sizes.txt
done
